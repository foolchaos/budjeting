spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/budjeting
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
vaadin:
  launch-browser: false
  production-mode: true
server:
  port: 8080
